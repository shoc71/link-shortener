{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server/index.js",
    "start": "concurrently \"nodemon server/index.js \" \"cd client && npm run start \" ",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "clean:node_modules": "find . -name 'node_modules' -type d -prune -exec rm -fr '{}' \\;",
    "clean:package-lock": "find . -name 'package-lock.json' -type f -delete",
    "clean:all": "npm run clean:package-lock && npm run clean:node_modules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.11"
  }
}
